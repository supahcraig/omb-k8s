apiVersion: v1
kind: Service
metadata:
  name: omb-worker
  labels:
    app: omb-worker
spec:
  clusterIP: None
  selector:
    app: omb-worker
  ports:
    - name: worker
      port: {{ .Values.worker.port }}
      targetPort: {{ .Values.worker.port }}
      protocol: TCP
