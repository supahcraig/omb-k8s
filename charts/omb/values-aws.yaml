storage:
  storageClassName: gp3
  createStorageClass: true

clusterAutoscaler:
  enabled: true
  # clusterName, region, and roleArn are engagement-specific — set them in
  # my-values.yaml, not here. Putting empty strings here causes them to
  # overwrite values from my-values.yaml when using -f values-aws.yaml.
